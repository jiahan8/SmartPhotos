package com.jiahan.smartcamera.settings

import kotlinx.serialization.Serializable

/**
 * Navigation route for [SettingsScreen]. Routes live in the feature package that owns them rather
 * than in one central hierarchy -- see `smartPhotosNavGraph`.
 */
@Serializable
data object SettingsRoute