package com.jiahan.smartcamera.explore

import kotlinx.serialization.Serializable

/**
 * Navigation route for [ExploreScreen]. Routes live in the feature package that owns them rather
 * than in one central hierarchy -- see `smartPhotosNavGraph`.
 */
@Serializable
data object ExploreRoute