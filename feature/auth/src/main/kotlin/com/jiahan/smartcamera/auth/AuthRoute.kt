package com.jiahan.smartcamera.auth

import kotlinx.serialization.Serializable

/**
 * Navigation route for [AuthScreen]. Routes live in the feature package that owns them rather than
 * in one central hierarchy -- see [com.jiahan.smartcamera.navigation.smartPhotosNavGraph].
 */
@Serializable
data object AuthRoute