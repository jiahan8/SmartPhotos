package com.jiahan.smartcamera.profile

import kotlinx.serialization.Serializable

/**
 * Navigation route for [ProfileScreen]. Routes live in the feature package that owns them rather
 * than in one central hierarchy -- see `smartPhotosNavGraph`.
 */
@Serializable
data object ProfileRoute