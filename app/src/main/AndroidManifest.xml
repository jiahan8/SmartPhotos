<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jiahan.smartcamera">


<!-- CameraX libraries require minSdkVersion 21, while this quickstart app
    supports low to 16. Needs to use overrideLibrary to make the merger tool
    ignore this conflict and import the libraries while keeping the app's lower
    minSdkVersion value. In code, will check SDK version, before calling CameraX
    APIs. -->
<!--    <uses-sdk-->
<!--        tools:overrideLibrary="-->
<!--          androidx.camera.camera2, androidx.camera.core,-->
<!--          androidx.camera.view, androidx.camera.lifecycle" />-->

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCamera">
        <activity android:name="com.jiahan.smartcamera.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.jiahan.smartcamera.kotlin.StillImageActivity"
            android:theme="@style/AppThemeNoActionBar"
            />

        <activity android:name="com.jiahan.smartcamera.preference.SettingsActivity" />
        <activity android:name="com.jiahan.smartcamera.SearchActivity" />
        <activity android:name="com.jiahan.smartcamera.ImageActivity"
            android:theme="@style/AppThemeNoActionBar"
            />


    </application>

</manifest>