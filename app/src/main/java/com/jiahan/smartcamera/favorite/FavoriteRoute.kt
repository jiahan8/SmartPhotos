package com.jiahan.smartcamera.favorite

import kotlinx.serialization.Serializable

/**
 * Navigation route for [FavoriteScreen]. Routes live in the feature package that owns them rather
 * than in one central hierarchy -- see [com.jiahan.smartcamera.navigation.smartPhotosNavGraph].
 */
@Serializable
data object FavoriteRoute