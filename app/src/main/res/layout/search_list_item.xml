<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="com.jiahan.smartcamera.Utils" />
        <import type="android.view.View" />
        <variable
            name="user"
            type="com.jiahan.smartcamera.UserData" />
        <variable
            name="viewmodel"
            type="com.jiahan.smartcamera.SearchViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:fresco="http://schemas.android.com/apk/res-auto"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:id="@+id/cl_search"
        android:clickable="true"
        android:focusable="true"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:onClick="@{(view)->viewmodel.setOnClick(view,user)}">

        <com.facebook.drawee.view.SimpleDraweeView
            android:id="@+id/iv_search_profile_pic"
            android:layout_width="50dp"
            android:layout_height="50dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            fresco:roundedCornerRadius="10dp"
            imageUri="@{user.id}" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_search_username"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            tools:text="username username username username username username username"
            app:layout_constraintStart_toEndOf="@id/iv_search_profile_pic"
            app:layout_constraintEnd_toStartOf="@id/iv_delete"
            app:layout_constraintTop_toTopOf="@id/iv_search_profile_pic"
            android:text="@{Utils.cutoffstring( user.imagetext )}"
            />


        <ImageView
            android:id="@+id/iv_delete"
            android:layout_width="16dp"
            android:layout_height="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:src="@drawable/ic_close_dark"
            android:onClick="@{(view)->viewmodel.setOnClickDelete(view,user)}"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>