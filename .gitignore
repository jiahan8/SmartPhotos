*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# JVM crash and JIT-replay dumps. The Gradle/Kotlin daemons drop these in the repo root, and they
# are large enough (tens of thousands of lines) to slip into a `git add -A` unnoticed.
hs_err_pid*.log
replay_pid*.log

# Firebase CLI cache & logs (keep rules/config tracked, ignore generated artifacts)
.firebase/
firebase-debug.log
firebase-debug.*.log
